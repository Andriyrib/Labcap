#include <stdio.h>

int main() {
    int n;
    printf("??????? ????????? ?????: ");
    scanf("%d", &n);

    double max, number;
    printf("??????? ????? ?????: ");
    scanf("%lf", &max);

    for (int i = 1; i < n; i++) {
        printf("??????? ???????? ?????: ");
        scanf("%lf", &number);

        if (number > max) {
            max = number;
        }
    }

    printf("??????????? ?????: %.2lf\n", max);

    return 0;
}